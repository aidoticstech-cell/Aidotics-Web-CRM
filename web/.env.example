# Base URL of the CRM **Express** API as seen by the Next.js **Node** process (rewrites /api/* here).
# If register/login show proxy/HTML errors, set this explicitly (try 127.0.0.1 if localhost misbehaves).
API_URL=http://127.0.0.1:4100
